---
interface Props {
  id: string;
  class?: string;
}

const { id, class: topSpacing = "mt-24 md:mt-28" } = Astro.props;
---

<section id={id} class={`mx-auto max-w-7xl px-4 sm:px-8 ${topSpacing}`}>
  <div class="mx-auto max-w-2xl lg:max-w-5xl lg:px-12">
    <slot />
  </div>
</section>
